# static-site-builder

start with
```yarn```

develop with
```yarn dev```

run as server
```cd docs && python -m SimpleHTTPServer 3000```

deploy
```yarn build && git add . && git commit -m 'build' && git push```
